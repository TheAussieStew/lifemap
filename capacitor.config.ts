import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eusaybia.app.lifemap',
  appName: 'lifemap',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
